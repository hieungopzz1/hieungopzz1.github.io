

<!-- Section Packages Start -->
<section class="packages p-4" id="packages">
    <div class="container">
        <h1 class="heading text-center fw-bolder text-warning">Packages</h1>
        <div class="row">
            {{#each lbx }}
            <div class="col-md-2">
                <div class="card">
                    <a href="lbx/show/{{_id}}">
                        <img width="100%" height="200vh" src="../images/{{image}}" alt="{{_id}}">
                    </a>
                    <div class="card-body">
                        <h3 class="fw-bolder">{{ name }}</h3>
                        <h5> $<strong>{{price}}</strong></h5>
                    </div>
                    <a class="btn btn-warning col-md-7 m-3" href="/cart">Add to cart</a>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>
<!-- Section Packages End -->